import React from "react";
import { Helmet } from "react-helmet";

const HomePage = () => (
  <div>
    <Helmet>
      <title>Home Page</title>
    </Helmet>
    <h1>Home Page</h1>
  </div>
);

export default HomePage;
